import React, { useEffect, useRef } from 'react'

export default function useScroll() {
  const isInScrollContainer = useRef(false);
  const hasScrolledHorizontally = useRef(false);
  const scrollContainer = useRef(null)
  useEffect(() => {
    
  }, [])
}
